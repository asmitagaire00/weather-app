A simple weather app based on openweathermap api.

## Usage
You need an api key from https://openweathermap.org/api.
Add config.js file in the project directory.

```javascript
export const config = {
    weatherApiKey: 'your_api_key',
};

